# Python
__pycache__/
*.py[cod]
*.so
*.egg-info/
.pytest_cache/
.ruff_cache/

# Virtualenvs
.venv/
venv/
env/

# uv lockfile we keep; the cache we don't
.uv-cache/

# IDE
.vscode/
.idea/
*.swp
.DS_Store

# Build artifacts
build/
dist/

# Pipeline outputs (regeneratable)
reports/
checkpoints/
*.png
*.json
!docs/**/*.png
!docs/**/*.json

# Large data — benchmarks come from external sources
benchmarks/external/

# Notebooks (we don't really use them, but keep stripped if we do)
*.ipynb_checkpoints
